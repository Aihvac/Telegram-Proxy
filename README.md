# MTG Telegram Proxy Auto-Installer

🚀 **نصب و راه‌اندازی خودکار MTProto Proxy (MTG) برای تلگرام بر روی سرور لینوکس (Ubuntu/Debian)**

---

## 📋 درباره پروژه

این اسکریپت به‌صورت خودکار، پروکسی سریع و امن **MTG (MTProto Proxy)** را بر روی سرور شما نصب، پیکربندی و فعال می‌کند.  
MTG به زبان Go نوشته شده و یکی از سریع‌ترین و پایدارترین پروکسی‌های تلگرام است.

---

## ✨ ویژگی‌ها

- 🔧 نصب خودکار و بدون نیاز به تنظیم دستی  
- 🔐 تولید خودکار Secret (در صورت خطا، تولید تصادفی انجام می‌شود)  
- 🚀 ساخت و فعال‌سازی سرویس systemd  
- 🔄 نصب مجدد بدون حذف دستی نسخه قبلی  
- 📊 نمایش لینک‌های آماده اتصال برای تلگرام  

---

## 🛠️ پیش‌نیازها

- سیستم عامل: Ubuntu 20/22/24 یا Debian 10+  
- دسترسی **sudo**  
- پورت **3128** آزاد باشد  

---

## 🚀 روش نصب

### روش 1: دانلود و اجرا
```bash
wget -O mtg.sh https://raw.githubusercontent.com/Aihvac/Telegram-Proxy/main/mtg.sh
chmod +x mtg.sh
sudo ./mtg.sh
```

### روش 2: اجرای تک‌خطی
```bash
bash <(wget -qO- https://raw.githubusercontent.com/Aihvac/Telegram-Proxy/main/mtg.sh)
```

---

## ⚙️ پیکربندی سفارشی

### تغییر دامنه
در ابتدای فایل `mtg.sh` مقدار زیر را تغییر دهید:
```bash
DOMAIN="yourdomain.com"
```

### تغییر پورت
در همان فایل مقدار زیر را تغییر دهید:
```bash
PORT="3128"
```

---

## 📊 مدیریت سرویس

بررسی وضعیت سرویس:
```bash
sudo systemctl status mtg
```

راه‌اندازی مجدد:
```bash
sudo systemctl restart mtg
```

توقف سرویس:
```bash
sudo systemctl stop mtg
```

مشاهده لاگ‌ها:
```bash
sudo journalctl -u mtg -f
```

---

## 🔗 دریافت لینک‌های پروکسی

دستور زیر را اجرا کنید:
```bash
mtg access /etc/mtg.toml
```

برای دریافت فقط لینک IPv4:
```bash
mtg access /etc/mtg.toml | jq -r '.ipv4.tg_url'
```

---

## 📁 مسیرهای مهم

| فایل | توضیح |
|------|--------|
| `/usr/local/bin/mtg` | فایل اجرایی MTG |
| `/etc/mtg.toml` | فایل تنظیمات اصلی |
| `/etc/systemd/system/mtg.service` | سرویس systemd |

---

## 🔧 رفع مشکلات احتمالی

### 🔹 سرویس اجرا نمی‌شود
بررسی لاگ‌ها:
```bash
sudo journalctl -u mtg -e
```
اگر پورت بسته است:
```bash
sudo ufw allow 3128
```

### 🔹 لینک‌ها نمایش داده نمی‌شوند
اطمینان حاصل کنید که:
- سرویس در حال اجراست  
- فایل `/etc/mtg.toml` وجود دارد  
- ابزار `jq` نصب شده است  

---

## 🛡️ امنیت

- Secret اختصاصی و تصادفی برای هر نصب  
- اجرای سرویس با کاربر محدود (`DynamicUser=true`)  
- دسترسی فقط به پورت مشخص‌شده  

---

## 📝 یادداشت‌ها

- در صورت اجرای مجدد، نسخه قبلی به‌صورت خودکار حذف و نسخه جدید جایگزین می‌شود.  
- سازگار با نسخه‌های جدید Ubuntu (از جمله 24.04).  
- نیازی به تنظیم دستی Go یا Git نیست — همه خودکار انجام می‌شود.  

---

## 🤝 مشارکت

اگر مشکلی یا پیشنهادی داری، در بخش **Issues** در GitHub مطرح کن.  
ستاره‌ی شما باعث دلگرمی ماست 🌟

---

## 📄 لایسنس

این پروژه تحت لایسنس MIT منتشر شده است.  
آزادید در هر پروژه‌ای از آن استفاده کنید.
