# MTG Telegram Proxy Auto-Installer

🚀 **یک اسکریپت خودکار برای نصب و راه‌اندازی MTG (MTProto Proxy) بر روی سرور لینوکس**

## 📋 درباره پروژه

این اسکریپت به صورت خودکار MTG (MTProto Proxy) را بر روی سرور شما نصب و پیکربندی می‌کند. MTG یکی از بهترین و سریع‌ترین پروکسی‌های تلگرام است که به زبان Go نوشته شده و عملکرد بسیار بالایی دارد.

## ✨ ویژگی‌های اسکریپت

- 🔧 **نصب خودکار**: تمام مراحل نصب به صورت خودکار انجام می‌شود
- 🔐 **تولید خودکار Secret**: Secret های MTProto به صورت خودکار تولید می‌شوند
- 🚀 **راه‌اندازی Systemd Service**: سرویس به صورت خودکار اجرا و فعال می‌شود
- 🔄 **پشتیبانی از نصب مجدد**: در صورت وجود نسخه قبلی، مشکلی پیش نمی‌آید
- 📊 **نمایش لینک‌های پروکسی**: لینک‌های آماده برای استفاده نمایش داده می‌شوند

## 🛠️ پیش‌نیازها

- سرور لینوکس (Ubuntu/Debian)
- دسترسی sudo
- اتصال اینترنت
- پورت 3128 آزاد باشد

## 🚀 نحوه استفاده

### روش 1: اجرای مستقیم از GitHub

```bash
wget -O mtg.sh https://raw.githubusercontent.com/Aihvac/Telegram-Proxy/refs/heads/main/mtg.sh
chmod +x mtg.sh
sudo ./mtg.sh
```

### روش 2: اجرای تک خطی

```bash
bash <(wget -qO- https://raw.githubusercontent.com/Aihvac/Telegram-Proxy/refs/heads/main/mtg.sh)
```

## ⚙️ پیکربندی

### تغییر دامنه

قبل از اجرای اسکریپت، می‌توانید متغیر `DOMAIN` را در فایل تغییر دهید:

```bash
DOMAIN="yourdomain.com"
```

### تغییر پورت

برای تغییر پورت، خط زیر را در اسکریپت ویرایش کنید:

```bash
bind-to = "0.0.0.0:3128"  # پورت دلخواه خود را جایگزین کنید
```

## 📊 مدیریت سرویس

### بررسی وضعیت سرویس
```bash
sudo systemctl status mtg
```

### راه‌اندازی مجدد سرویس
```bash
sudo systemctl restart mtg
```

### مشاهده لاگ‌ها
```bash
sudo journalctl -u mtg -f
```

### توقف سرویس
```bash
sudo systemctl stop mtg
```

## 🔗 دریافت لینک‌های پروکسی

```bash
mtg access /etc/mtg.toml
```

یا برای دریافت فقط لینک‌ها:

```bash
mtg access /etc/mtg.toml | jq -r '.ipv4.tg_url'
mtg access /etc/mtg.toml | jq -r '.ipv6.tg_url'
```

## 📁 فایل‌های مهم

- **باینری**: `/usr/local/bin/mtg`
- **فایل پیکربندی**: `/etc/mtg.toml`
- **سرویس Systemd**: `/etc/systemd/system/mtg.service`

## 🔧 حل مشکلات

### مشکل در اتصال

1. بررسی کنید که پورت 3128 باز باشد:
```bash
sudo ufw allow 3128
```

2. وضعیت سرویس را بررسی کنید:
```bash
sudo systemctl status mtg
```

### مشکل در دریافت لینک‌ها

اگر دستور `mtg access` کار نمی‌کند، مطمئن شوید که:
- سرویس mtg در حال اجرا است
- فایل پیکربندی موجود است
- jq نصب شده است

## 🛡️ امنیت

- Secret های جدید برای هر نصب تولید می‌شوند
- سرویس با کاربر محدود اجرا می‌شود (DynamicUser=true)
- فقط دسترسی‌های ضروری اعطا می‌شوند

## 📝 یادداشت‌ها

- اسکریپت برای سیستم‌های مبتنی بر Debian/Ubuntu بهینه شده است
- در صورت وجود نسخه قبلی، به صورت خودکار به‌روزرسانی می‌شود
- تمام وابستگی‌های مورد نیاز خودکار نصب می‌شوند

## 🤝 مشارکت

برای گزارش مشکلات یا پیشنهادات، لطفاً به صفحه Issues مراجعه کنید.

## 📄 لایسنس

این پروژه تحت لایسنس MIT منتشر شده است.

---

**⭐ اگر این پروژه برای شما مفید بود، لطفاً ستاره بدهید!**
